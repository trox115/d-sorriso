const initState = {
    clientes: [
        {nome: 'António Fernandes', morada: 'Rua do padrão'}
    ]
}

const clientReducer = (state= initState, action) => {
    return state;
}

export default clientReducer;